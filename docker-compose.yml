version: '2'

services:
  huemqtt:
    hostname: huemqtt
    image: pharndt/hue2mqtt:${VERSION}
    build:
      context: app-go
    volumes:
      - ./production/config:/var/lib/hue2mqtt:ro
    restart: "no"
